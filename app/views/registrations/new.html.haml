%h2 Sign Up
	= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
		= devise_error_messages!

		%p
			= f.label :name
			= f.text_field :name

		%p
			= f.label :email
			= f.email_field :email

		%p
			= f.submit 'Sign Up'
	